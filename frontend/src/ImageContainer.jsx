import React from 'react';

const ImageContainer = (props) => (
  // Needs to get through all apiData paths and show the needed saved files
  // Obviously a wip
  // <div>
  //   <img src={apiData && apiData !== 'fetching' ? `http://localhost:5000/${apiData[0]}` : null} />
  //
  // </div>
  <div />
);

export default ImageContainer;
